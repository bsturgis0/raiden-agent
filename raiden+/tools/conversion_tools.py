@tool
def convert_document(input_path: str, output_format: str) -> str:
    """Converts documents between formats (doc, docx, pdf, etc)"""

@tool
def convert_image(input_path: str, output_format: str) -> str:
    """Converts images between formats"""
