@tool
def post_tweet(content: str, media_path: Optional[str] = None) -> str:
    """Posts content to Twitter/X"""

@tool
def linkedin_post(content: str, visibility: str = "public") -> str:
    """Creates LinkedIn posts"""
