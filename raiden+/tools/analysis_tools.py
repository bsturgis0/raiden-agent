@tool
def analyze_csv(file_path: str) -> str:
    """Performs statistical analysis on CSV data"""

@tool
def generate_report(data: dict, template: str = "standard") -> str:
    """Generates formatted reports from data"""
