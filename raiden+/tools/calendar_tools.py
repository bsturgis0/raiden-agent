@tool
def create_event(title: str, start_time: str, duration: int) -> str:
    """Creates calendar events"""

@tool
def find_free_slots(date: str) -> str:
    """Finds available time slots in calendar"""
